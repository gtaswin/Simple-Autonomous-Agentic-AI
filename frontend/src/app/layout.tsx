import type { Metadata, Viewport } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Autonomous AI Assistant',
  description: 'Advanced autonomous AI agent with continuous reasoning and multi-agent collaboration',
  keywords: ['AI', 'autonomous', 'assistant', 'agent', 'reasoning', 'collaboration'],
  authors: [{ name: 'AI Research Team' }],
  icons: {
    icon: '/favicon.ico',
    shortcut: '/favicon-16x16.png',
    apple: '/apple-touch-icon.png',
  },
}

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  themeColor: '#0A0A0F',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.className} min-h-screen bg-gray-950`}>
        {children}
      </body>
    </html>
  )
}